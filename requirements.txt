fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas==2.2.3
openpyxl==3.1.5
pypdf==5.1.0
pytest==8.3.4
httpx==0.28.1
langgraph==0.6.11

# Hybrid retrieval (app/rag). ParadeDB speaks plain Postgres: psycopg is the
# only client needed, and vectors go over the wire as pgvector text literals,
# so there is no pgvector Python package here.
psycopg[binary]==3.2.3
psycopg-pool==3.2.3
# Dense encoder. gte-base-en-v1.5 is 768-dim and ships custom modelling code on
# the Hub, so sentence-transformers loads it with trust_remote_code.
sentence-transformers==3.3.1
torch==2.5.1
einops==0.8.0
